import 'package:flutter/material.dart';

class AppColors {
  // Verdes principales
  static const Color verdeOscuro = Color(0xFF00707C); // para titulos,iconos , botones y barra de navegacion.
  static const Color verdeMedio = Color(0xFF05AABC);
  static const Color verdeEsmeralda = Color(0xFF0ECDE1);

  static const Color grisBoton = Color(0xFF62748E); // para color boton secundario (ejemplo boton rechazar).

  // Fondos
  static const Color fondoBlanco = Color(0xFFFFFFFF); // fondo general de la app. y algunas tarjetas y iconos
  static const Color fondoGrisClaro = Color(0xFFF1F5F9); // Principalmente lo uso para fondo de los botones o algunas tarjetas pero no lo uso mucho como fondo general.
  static const Color fondoGrisMedio = Color(0xFF71717B); // uso para algunos fondos de tarjetas.
  static const Color fondoGrisOscuro = Color(0xFF27272A); // uso para iconos y detallitos.

  // Letras
  static const Color blancoLetra = Color(0xFFFFFFFF);
  static const Color grisLetra = Color(0xFF52525C);
  static const Color grisOscuroLetra = Color(0xFF18181B);

  // Estados - estos colores no van a cambiar
  static const Color azul = Color(0xFF2B7FFF);
  static const Color naranja = Color(0xFFFF692A);
  static const Color violeta = Color(0xFF8E51FF);
  static const Color amarilloBrillante = Color(0xFFFFD230);
  static const Color verde = Color(0xFF36BBA7);
  static const Color rojo = Color(0xFFFB2C36);
}
